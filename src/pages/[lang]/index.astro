---
import Layout from "../../layouts/Layout.astro";
import YouCanScroll from "../../components/YouCanScroll.astro";
import Carousel3D from "../../components/Carousel3D.astro";
import { BentoGrid } from "../../components/ui/bento-grid";
import StackingCards from "../../components/ui/stacking-card";
import { SITE_DESCRIPTION } from "../../consts";
import { getLangFromUrl } from "../../i18n/translate";

const lang = getLangFromUrl(Astro.url);

const copy =
    lang === "en"
        ? {
              badge: "Technology studio",
              heroTitle: "We create solutions We build software",
              heroTitleLine1: "We create solutions",
              heroTitleLine2: "We build software",
              heroText: "We build digital products with strategy, design, and engineering to help your business scale.",
              heroPrimary: "Start a project",
              heroSecondary: "View services",
              heroMetaA: "Consulting and Product",
              heroMetaB: "In-house software team",
              heroGapTitle: "",
              servicesTitle: "What can we build together?",
              servicesSubtitle: "Examples",
              processTitle: "How it works",
              processSubtitle: "Work process",
              portfolioTitle: "Your industry could be next",
              portfolioSubtitle: "Success stories",
              withUsSubtitle: "Solutions Squad",
              teamTitle: "Our team",
              teamText: "Product designers, engineers, and growth specialists focused on real outcomes.",
              teamAction: "View founders",
              ctaText: "Join the future of autonomous software development.",
              ctaAction: "Schedule a meeting",
              endTitle: "Ready to digitize your business?",
              endPrimary: "Let's start",
                            endSecondary: "Learn more about us",
          }
        : {
              badge: "Estudio de tecnología",
              heroTitle: "La mejor forma de Creando sistemas",
              heroTitleLine1: "La mejor forma de",
              heroTitleLine2: "Creando sistemas",
              heroText: "Somos expertos en desarrollo de software y diseño de experiencias digitales. Creamos todo tipo de aplicaciones con código propio y soluciones a medida.",
              heroPrimary: "Completar formulario",
              heroSecondary: "Whatsapp directo",
              heroMetaA: "",
              heroMetaB: "",
              heroGapTitle: "",
              servicesTitle: "¿Qué podemos crear juntos?",
              servicesSubtitle: "Ejemplos",
              processTitle: "Como funciona",
              processSubtitle: "Proceso de trabajo",
              portfolioTitle: "Ya lo hicimos. Tu industria puede ser la proxima",
              portfolioSubtitle: "Casos reales",
              withUsSubtitle: "Equipo de Soluciones",
              teamTitle: "Nuestro equipo",
              teamText: "Disenadores, developers y especialistas en crecimiento enfocados en resultados reales.",
              teamAction: "Ver el equipo",
              ctaText: "Sumate al futuro del desarrollo de software autonomo.",
              ctaAction: "Contáctanos",
              endTitle: "Listo para digitalizar tu negocio?",
              endPrimary: "Comencemos",
              endSecondary: "Conoce mas sobre nosotros",
          };

const serviceItems =
    lang === "en"
        ? [
              {
                  icon: "app",
                  title: "App and web systems",
                  text: "Order, stock, shift, and customer management. Built around your workflow.",
              },
              {
                  icon: "dashboard",
                  title: "Dashboards and reports",
                  text: "Clear real-time business data for better decisions.",
              },
              {
                  icon: "automation",
                  title: "Automation",
                  text: "Remove repetitive tasks so your team can focus on what matters.",
              },
              {
                  icon: "commerce",
                  title: "E-commerce",
                  text: "Online stores integrated with payment, invoicing, logistics, and stock.",
              },
              {
                  icon: "ai",
                  title: "Artificial intelligence",
                  text: "Chatbots, demand prediction, and data analysis with AI for your business.",
              },
              {
                  icon: "integrations",
                  title: "Integrations",
                  text: "We connect tools like SAP, Tango, MercadoPago, WhatsApp, and your current stack.",
              },
          ]
        : [
              {
                  icon: "app",
                  title: "App y sistemas web",
                  text: "Gestion de pedidos, stock, turnos y clientes. Lo que necesites, a medida.",
              },
              {
                  icon: "dashboard",
                  title: "Dashboards e informes",
                  text: "Datos de tu negocio claros y en tiempo real para tomar mejores decisiones.",
              },
              {
                  icon: "automation",
                  title: "Automatizacion",
                  text: "Elimina tareas manuales y repetitivas para que tu equipo se enfoque en lo importante.",
              },
              {
                  icon: "commerce",
                  title: "E-commerce",
                  text: "Tiendas online integradas con medios de pago, facturacion, logistica y stock.",
              },
              {
                  icon: "ai",
                  title: "Inteligencia artificial",
                  text: "Chatbots, prediccion de demanda y analisis de datos con IA aplicada a tu negocio.",
              },
              {
                  icon: "integrations",
                  title: "Integraciones",
                  text: "Conectamos SAP, Tango, MercadoPago, WhatsApp y lo que ya uses hoy.",
              },
          ];

const steps = [
    {
        number: "01",
        icon: "discovery",
        title: "Descubrimiento y Analisis",
        text: "Comenzamos entendiendo los objetivos de tu negocio y los requerimientos tecnicos. Definimos juntos el alcance del proyecto y te enviamos una cotizacion personalizada.",
    },
    {
        number: "02",
        icon: "development",
        title: "Desarrollo",
        text: "Desarrollamos tu solucion a medida utilizando tecnologias modernas y practicas agiles. Recibis actualizaciones de progreso en cada sprint.",
    },
    {
        number: "03",
        icon: "testing",
        title: "Pruebas e implementacion",
        text: "Antes del lanzamiento, se validan la funcionalidad, el rendimiento y la usabilidad. Aseguramos una implementacion fluida y sin interrupciones.",
    },
    {
        number: "04",
        icon: "support",
        title: "Soporte continuo",
        text: "Despues de la entrega, brindamos mantenimiento, actualizaciones y optimizacion continua. Tu sistema se mantiene estable y preparado para crecer junto a tu negocio.",
    },
];

const projects = [
    {
        image: "/projects/hexadevs.png",
        title: "Hexadevs",
        text: "Sitio institucional con foco en conversion.",
    },
    {
        image: "/images/olympo-phone-screen.png",
        title: "Catálogo Digital",
        text: "Catálogo premium: Estética moderna y conversión ágil.",
    },
    {
        image: "/projects/todo-app.svg",
        title: "Taskflow",
        text: "Sistema de tareas y productividad para equipos.",
    },
    {
        image: "/projects/flashcards.svg",
        title: "Learning Hub",
        text: "Plataforma educativa para aprendizaje dinamico.",
    },
    {
        image: "/projects/bunny-js.svg",
        title: "Bunny JS",
        text: "Web app ligera orientada a performance y conversion.",
    },
    {
        image: "/projects/animal.svg",
        title: "Animal UI",
        text: "Landing visual con identidad de marca y narrativa clara.",
    },
];

const stackingProjects = [
    {
        title: "Descubrimiento y análisis",
        subtitle: "Comenzamos entendiendo los objetivos de tu negocio y los requerimientos técnicos. Definimos juntos el alcance del proyecto y te enviamos una cotización personalizada.",
        description: "",
        link: "/video_card1.mp4",
        color: "#3b82f6",
    },
    {
        title: "Desarrollo",
        subtitle: "Desarrollamos tu solución a medida utilizando tecnologías modernas y prácticas ágiles. Recibís actualizaciones de progreso en cada sprint.",
        description: "",
        link: "https://images.unsplash.com/photo-1561070791-2526d30994b5?w=800&auto=format&fit=crop",
        color: "#6d28d9",
    },
    {
        title: "Pruebas e implementación",
        subtitle: "Antes del lanzamiento, se validan la funcionalidad, el rendimiento y la usabilidad. Aseguramos una implementación fluida y sin interrupciones.",
        description: "",
        link: "https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?w=800&auto=format&fit=crop",
        color: "#94a3b8",
    },
    {
        title: "Lanzamiento y Soporte",
        subtitle: "Después de la entrega, brindamos mantenimiento, actualizaciones y optimización continua. Tu sistema se mantiene estable y preparado para crecer junto a tu negocio.",
        description: "",
        link: "https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?w=800&auto=format&fit=crop",
        color: "#38bdf8",
    },
];

---

<Layout title="CROBF" description={SITE_DESCRIPTION}>
    <main class="home">
        <section class="hero-intro">
            <div class="hero-intro-content">
                <h1 class="hero-title">
                    {
                        lang === "es" ? (
                            <>
                                <span class="hero-title__line">Creando sistemas</span>
                                <span class="hero-title__line hero-title__swap" data-hero-bottom-swap>
                                    <span class="hero-title__swap-track">
                                        <span class="hero-title__swap-item">
                                            <span class="hero-left-rotate-wrap">
                                                <span
                                                    class="hero-left-rotate"
                                                    data-hero-left-rotate
                                                    data-hero-words="para solucionar|para innovar|para simplificar|para asegurar"
                                                >
                                                    para solucionar
                                                </span>
                                            </span>
                                        </span>
                                    </span>
                                </span>
                            </>
                        ) : (
                            <>
                                <span class="hero-title__line">{copy.heroTitleLine1}</span>
                                <span class="hero-title__line">{copy.heroTitleLine2}</span>
                            </>
                        )
                    }
                </h1>
                <p class="subtitle">{copy.heroText}</p>
                <div class="actions">
                    <a href={`/${lang}/contact#contact_form`} class="btn btn-ghost" onclick={`window.location.href=location.origin + '/${lang}/contact#contact_form'; return false;`} onmousedown={`window.location.href=location.origin + '/${lang}/contact#contact_form'; return false;`}>{copy.heroPrimary}</a>
                    <a href="#services" class="btn btn-primary">{copy.heroSecondary}</a>
                </div>
            </div>
        </section>

        <Carousel3D />

        <!-- services: Ejemplos / ¿Qué podemos crear juntos? -->
        <section class="services" id="services" style="margin-top: -28rem;">
            <p class="eyebrow" data-reveal style="--reveal-delay: 0ms;">{copy.servicesSubtitle}</p>
            <h2 data-reveal style="--reveal-delay: 50ms;">{copy.servicesTitle}</h2>
            <div data-reveal style="--reveal-delay: 90ms; margin-top: 0.75rem;">
                <BentoGrid client:load items={serviceItems} />
            </div>
        </section>

        <section class="parallax-flow" id="parallax-flow" aria-hidden="true"></section>

        <section class="hero" id="inicio">
            <div class="hero-visual" aria-hidden="true" data-reveal="right" style="--reveal-delay: 80ms;">
            </div>
        </section>

        <!-- services moved above to sit under the carousel -->

        <!-- Stacking Cards Section -->
        <section class="stacking-cards-section">
            <p class="stacking-cards-eyebrow" data-reveal style="--reveal-delay: 0ms;">Proceso de trabajo</p>
            <h2 class="stacking-cards-title" data-reveal style="--reveal-delay: 40ms;">Como funciona</h2>
            <StackingCards client:load projects={stackingProjects} />
        </section>

        <section class="portfolio" id="casos-exito">
            <p class="eyebrow" data-reveal>{copy.portfolioSubtitle}</p>
            <h2 data-reveal style="--reveal-delay: 40ms;">{copy.portfolioTitle}</h2>
            <div class="portfolio-row">
                {
                    projects.map((project, index) => (
                        <article class="project-card" data-reveal style={`--reveal-delay: ${70 + index * 45}ms;`}>
                            <div class="project-media">
                                <img src={project.image} alt={project.title} loading="lazy" draggable="false" />
                            </div>
                            <h3>{project.title}</h3>
                            <p>{project.text}</p>
                        </article>
                    ))
                }
            </div>
            <div class="portfolio-cta" data-reveal style="--reveal-delay: 120ms;">
                <a href={`/${lang}/projects`} class="btn btn-primary">Ver más</a>
            </div>
        </section>

        <YouCanScroll />

        <section class="team" id="about-us">
            <div class="team-copy" data-reveal>
                <p class="eyebrow">{copy.withUsSubtitle}</p>
                <h2>{copy.teamTitle}</h2>
                <p>{copy.teamText}</p>
                <a href={`/${lang}/about`} class="btn btn-primary">{copy.teamAction}</a>
            </div>
        </section>

        <section class="video-cta-banner" data-reveal>
            <div class="video-cta-container">
                <!-- Video Background -->
                <div class="video-background">
                    <video autoplay muted loop playsinline class="video-element">
                        <source src="/video_formulario.mp4" type="video/mp4">
                    </video>
                </div>

                <!-- Content -->
                <div class="video-cta-content">
                    <h2 class="video-cta-title">{copy.ctaText}</h2>
                    <a href={`/${lang}/contact#contact_form`} class="btn btn-primary video-cta-btn">{copy.ctaAction}</a>
                </div>
            </div>
        </section>

        <script is:inline>
            const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
            const portfolioRow = document.querySelector(".portfolio-row");
            const heroLeftRotate = document.querySelector("[data-hero-left-rotate]");
            const WORD_LIST_DURATION_MS = 1200;
            const SELECTOR_DURATION_MS = 500;

            if (!reduceMotion) {
                if (heroLeftRotate instanceof HTMLElement) {
                    const words = (heroLeftRotate.dataset.heroWords || "")
                        .split("|")
                        .map((word) => word.trim())
                        .filter(Boolean);

                    if (words.length > 1) {
                        let wordIndex = 0;

                        const changeWord = () => {
                            heroLeftRotate.classList.add("is-leaving");

                            window.setTimeout(() => {
                                wordIndex = (wordIndex + 1) % words.length;
                                heroLeftRotate.style.transition = "none";
                                heroLeftRotate.classList.add("is-hidden");
                                heroLeftRotate.textContent = words[wordIndex];
                                heroLeftRotate.classList.remove("is-leaving");
                                heroLeftRotate.classList.add("is-entering");

                                void heroLeftRotate.offsetWidth;
                                heroLeftRotate.style.transition = "";
                                heroLeftRotate.classList.remove("is-hidden");

                                window.setTimeout(() => {
                                    heroLeftRotate.classList.remove("is-entering");
                                }, SELECTOR_DURATION_MS);
                            }, SELECTOR_DURATION_MS / 2);
                        };

                        window.setTimeout(() => {
                            window.setInterval(changeWord, WORD_LIST_DURATION_MS + 1000);
                        }, WORD_LIST_DURATION_MS + 450);
                    }
                }
            }

            if (portfolioRow instanceof HTMLElement) {
                const DRAG_SPEED = 1.28;
                let isDragging = false;
                let startX = 0;
                let startScrollLeft = 0;
                let draggedDistance = 0;
                let velocity = 0;
                let lastTime = 0;
                let momentumFrame = 0;

                const stopMomentum = () => {
                    if (!momentumFrame) return;
                    window.cancelAnimationFrame(momentumFrame);
                    momentumFrame = 0;
                    portfolioRow.classList.remove("is-momentum");
                };

                const runMomentum = () => {
                    if (Math.abs(velocity) < 0.015) {
                        velocity = 0;
                        momentumFrame = 0;
                        portfolioRow.classList.remove("is-momentum");
                        return;
                    }

                    portfolioRow.scrollLeft += velocity * 16;
                    velocity *= 0.92;
                    momentumFrame = window.requestAnimationFrame(runMomentum);
                };

                const startDrag = (event) => {
                    if (event.pointerType !== "mouse") return;
                    if (event.button !== 0) return;
                    stopMomentum();
                    isDragging = true;
                    startX = event.clientX;
                    startScrollLeft = portfolioRow.scrollLeft;
                    draggedDistance = 0;
                    velocity = 0;
                    lastTime = performance.now();
                    portfolioRow.classList.add("is-dragging");
                    portfolioRow.setPointerCapture(event.pointerId);
                };

                const moveDrag = (event) => {
                    if (!isDragging) return;
                    const now = performance.now();
                    const deltaX = event.clientX - startX;
                    draggedDistance = Math.max(draggedDistance, Math.abs(deltaX));

                    const nextScrollLeft = startScrollLeft - deltaX * DRAG_SPEED;
                    const dt = Math.max(1, now - lastTime);
                    velocity = (nextScrollLeft - portfolioRow.scrollLeft) / dt;

                    portfolioRow.scrollLeft = nextScrollLeft;
                    lastTime = now;

                    if (draggedDistance > 6) {
                        event.preventDefault();
                    }
                };

                const endDrag = (event) => {
                    if (!isDragging) return;
                    isDragging = false;
                    portfolioRow.classList.remove("is-dragging");
                    if (portfolioRow.hasPointerCapture(event.pointerId)) {
                        portfolioRow.releasePointerCapture(event.pointerId);
                    }

                    if (draggedDistance > 6) {
                        portfolioRow.classList.add("is-momentum");
                        momentumFrame = window.requestAnimationFrame(runMomentum);
                    }
                };

                portfolioRow.addEventListener("pointerdown", startDrag);
                portfolioRow.addEventListener("pointermove", moveDrag);
                portfolioRow.addEventListener("pointerup", endDrag);
                portfolioRow.addEventListener("pointercancel", endDrag);
                portfolioRow.addEventListener("pointerleave", endDrag);
                portfolioRow.addEventListener("dragstart", (event) => event.preventDefault());
            }

            if (!reduceMotion) {
                document.querySelectorAll(".tilt-card").forEach((card) => {
                    const layers = card.querySelectorAll(".tilt-layer");
                    const maxRotate = 7;

                    const reset = () => {
                        card.style.transform = "perspective(900px) rotateX(0deg) rotateY(0deg) translateY(0px)";
                        layers.forEach((layer) => {
                            const depth = Number(layer.getAttribute("data-depth") || 0);
                            layer.style.transform = `translateZ(${depth}px)`;
                        });
                    };

                    reset();

                    card.addEventListener("mousemove", (event) => {
                        const rect = card.getBoundingClientRect();
                        const px = (event.clientX - rect.left) / rect.width - 0.5;
                        const py = (event.clientY - rect.top) / rect.height - 0.5;

                        const ry = px * maxRotate * 2;
                        const rx = -py * maxRotate * 2;

                        card.style.transform = `perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-2px)`;

                        layers.forEach((layer) => {
                            const depth = Number(layer.getAttribute("data-depth") || 0);
                            const lx = -px * depth * 0.5;
                            const ly = -py * depth * 0.5;
                            layer.style.transform = `translate3d(${lx}px, ${ly}px, ${depth}px)`;
                        });
                    });

                    card.addEventListener("mouseleave", reset);
                    card.addEventListener("blur", reset, true);
                });
            }

            const revealTargets = document.querySelectorAll("[data-reveal]:not([data-reveal-card])");
            const revealCards = document.querySelectorAll("[data-reveal-card]");
            const processSection = document.querySelector(".process");
            const servicesSection = document.querySelector(".services");

            if (reduceMotion) {
                revealTargets.forEach((target) => target.classList.add("is-visible"));
                revealCards.forEach((target) => target.classList.add("is-visible"));
                if (processSection instanceof HTMLElement) {
                    processSection.classList.add("is-line-active");
                }
            } else {
                const revealObserver = new IntersectionObserver(
                    (entries, observer) => {
                        entries.forEach((entry) => {
                            if (!entry.isIntersecting) return;
                            entry.target.classList.add("is-visible");
                            observer.unobserve(entry.target);
                        });
                    },
                    {
                        threshold: 0.05,
                        rootMargin: "0px 0px -1% 0px",
                    }
                );

                revealTargets.forEach((target) => revealObserver.observe(target));

                if (servicesSection instanceof HTMLElement && revealCards.length) {
                    const servicesCardsObserver = new IntersectionObserver(
                        (entries, observer) => {
                            entries.forEach((entry) => {
                                if (!entry.isIntersecting) return;
                                revealCards.forEach((card, index) => {
                                    window.setTimeout(() => {
                                        card.classList.add("is-visible");
                                    }, index * 920);
                                });
                                observer.unobserve(servicesSection);
                            });
                        },
                        {
                            threshold: 0.28,
                            rootMargin: "0px 0px -6% 0px",
                        }
                    );

                    servicesCardsObserver.observe(servicesSection);
                }

                if (processSection instanceof HTMLElement) {
                    const processObserver = new IntersectionObserver(
                        (entries, observer) => {
                            entries.forEach((entry) => {
                                if (!entry.isIntersecting) return;
                                processSection.classList.add("is-line-active");
                                observer.unobserve(processSection);
                            });
                        },
                        {
                            threshold: 0.38,
                            rootMargin: "0px 0px -8% 0px",
                        }
                    );

                    processObserver.observe(processSection);
                }

            }
        </script>
    </main>
</Layout>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800;900&display=swap');

    .home {
        width: min(1120px, calc(100% - 2.2rem));
        margin-inline: auto;
        padding: 1.2rem 0 5rem;
        display: grid;
        gap: 1.8rem;
    }

    [data-reveal] {
        opacity: 0;
        transform: translate3d(0, var(--reveal-y, 22px), 0) scale(0.998);
        filter: none;
        transition:
            opacity var(--reveal-opacity-duration, 1450ms) cubic-bezier(0.16, 1, 0.3, 1),
            transform var(--reveal-transform-duration, 2100ms) cubic-bezier(0.16, 1, 0.3, 1),
            filter var(--reveal-opacity-duration, 1450ms) cubic-bezier(0.16, 1, 0.3, 1);
        transition-delay: var(--reveal-delay, 0ms);
        will-change: opacity, transform, filter;
    }

    [data-reveal="left"] {
        transform: translate3d(-14px, 6px, 0) scale(0.998);
    }

    [data-reveal="right"] {
        transform: translate3d(14px, 6px, 0) scale(0.998);
    }

    [data-reveal].is-visible {
        opacity: 1;
        transform: translate3d(0, 0, 0) scale(1);
        filter: blur(0);
    }

    :global(body) {
        overflow-x: hidden;
    }

    .eyebrow {
        margin: 0 0 0.9rem;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        font-size: 0.72rem;
        color: rgba(23, 30, 48, 0.72);
    }

    .hero-intro {
        position: relative;
        display: grid;
        grid-template-columns: 1fr;
        gap: 1.2rem;
        align-items: center;
        min-height: auto;
        padding-top: 7.2rem;
        padding-bottom: 0;
        isolation: isolate;
        justify-items: center;
        text-align: center;
        width: 100%;
        z-index: 1000;
    }

    .hero-intro-content {
        width: 100%;
        display: grid;
        justify-items: center;
        text-align: center;
        gap: 1.5rem;
        row-gap: 1.8rem;
    }

    /* Mover el bloque hero hacia la izquierda en pantallas grandes */
    @media (min-width: 820px) {
        .hero-intro {
            justify-items: center;
            text-align: center;
            padding-left: 1.3rem;
            padding-right: 1.3rem;
        }

        .hero-intro-content {
            width: min(920px, 85%);
            margin-inline: auto;
        }
    }

    .hero {
        position: relative;
        display: grid;
        grid-template-columns: 1fr;
        gap: 1.2rem;
        align-items: center;
        min-height: auto;
        padding-top: 0;
        margin-top: 0;
        isolation: isolate;
        justify-items: center;
        text-align: center;
    }

    .parallax-flow {
        width: 100%;
        min-height: 0;
        overflow: hidden;
        display: none;
    }

    .hero-copy,
    .hero-visual {
        position: relative;
        z-index: 2;
    }

    .hero-copy {
        width: min(900px, 100%);
        display: grid;
        justify-items: center;
        text-align: center;
        margin-inline: auto;
    }

    h1 {
        font-size: clamp(2rem, 4.6vw, 4.2rem);
        line-height: 1.02;
        margin: 0;
        max-width: 100%;
    }

    .home h1,
    .home h2,
    .home h3 {
        text-align: center;
        margin-inline: auto;
    }

    .hero-title {
        display: grid;
        gap: 0.05em;
        font-family: 'Plus Jakarta Sans', "Inter", "Inter Tight", "-apple-system", "BlinkMacSystemFont", "Segoe UI", sans-serif;
        font-weight: 900;
        font-size: clamp(2.2rem, 8vw, 90px);
        line-height: 0.95;
        letter-spacing: -0.035em;
        margin: 0;
        max-width: 100%;
        text-transform: uppercase;
        color: rgba(60, 60, 60, 0.98);
        word-break: break-word;
        overflow-wrap: break-word;
    }

    :global(html.dark) .hero-title {
        color: rgb(255, 255, 255);
    }

    .hero-title__line {
        display: block;
        white-space: normal;
        word-break: break-word;
    }

    .hero-title__swap {
        position: relative;
        display: inline-block;
    }

    .hero-title__swap-track {
        display: block;
        transform: none;
    }

    .hero-title__swap.is-shifted .hero-title__swap-track {
        transform: none;
    }

    .hero-title__swap-item {
        display: none;
    }
    
    .hero-title__swap-item:first-child {
        display: inline;
    }

    .hero-left-rotate {
        display: block;
        width: 100%;
        text-align: center;
        will-change: transform, opacity;
    }

    .hero-left-rotate-wrap {
        display: inline-block;
        min-width: 0;
        max-width: 100%;
        overflow: clip;
        contain: paint;
    }

    .hero-left-rotate.is-leaving {
        opacity: 0;
        transform: translateX(-0.28em);
        transition: transform 250ms ease, opacity 250ms ease;
    }

    .hero-left-rotate.is-entering {
        opacity: 0;
        transform: translateX(0.28em);
        transition: transform 250ms ease, opacity 250ms ease;
    }

    .hero-left-rotate.is-hidden {
        opacity: 0;
        transform: translateX(0);
    }

    h2 {
        font-size: clamp(1.7rem, 3vw, 2.8rem);
        margin: 0;
        max-width: 20ch;
        line-height: 1.1;
    }

    .subtitle {
        color: rgba(23, 30, 48, 0.78);
        max-width: 52ch;
        margin: 0;
    }

    .hero-meta {
        margin: 0.95rem 0 0;
        padding: 0;
        list-style: none;
        display: grid;
        gap: 0.34rem;
        font-size: 1.03rem;
        line-height: 1.3;
        font-weight: 500;
        color: rgba(31, 42, 72, 0.72);
        justify-items: center;
    }

    .hero-meta li::before {
        content: "+ ";
        color: #4d6eff;
        font-weight: 700;
    }

    .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.7rem;
        justify-content: center;
        margin: 0;
    }

    .btn {
        padding: 1rem 1.8rem;
        border-radius: 999px;
        font-size: 1rem;
        letter-spacing: 0.02em;
        text-decoration: none;
        border: 1px solid transparent;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        transform-style: preserve-3d;
        will-change: transform;
    }

    .btn:hover {
        transform: translateY(-2px) scale(1.02);
    }

    .btn-primary {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.03) 100%);
        color: rgba(16, 25, 49, 0.96);
        box-shadow: 
            0 10px 30px -10px rgba(0, 0, 0, 0.1),
            inset 0 1px 1px rgba(255, 255, 255, 0.1),
            inset 0 -1px 2px rgba(0, 0, 0, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.08);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
    }

    .btn-primary:hover,
    .btn-primary:focus,
    .btn-primary:active {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.12) 0%, rgba(255, 255, 255, 0.05) 100%);
        box-shadow: 
            0 20px 40px -10px rgba(0, 0, 0, 0.2),
            inset 0 1px 1px rgba(255, 255, 255, 0.2);
        border-color: rgba(255, 255, 255, 0.2);
        color: rgba(16, 25, 49, 1) !important;
    }

    .btn-primary:visited {
        color: rgba(16, 25, 49, 0.96) !important;
    }

    .btn-ghost {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0.03) 100%);
        box-shadow: 
            0 10px 30px -10px rgba(0, 0, 0, 0.1),
            inset 0 1px 1px rgba(255, 255, 255, 0.1),
            inset 0 -1px 2px rgba(0, 0, 0, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.08);
        color: rgba(16, 25, 49, 0.96) !important;
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
    }

    .btn-ghost:hover,
    .btn-ghost:focus,
    .btn-ghost:active {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.12) 0%, rgba(255, 255, 255, 0.05) 100%);
        border-color: rgba(255, 255, 255, 0.2);
        box-shadow: 
            0 20px 40px -10px rgba(0, 0, 0, 0.2),
            inset 0 1px 1px rgba(255, 255, 255, 0.2);
        color: rgba(16, 25, 49, 1) !important;
    }

    .btn-ghost:visited,
    .btn-ghost:link {
        color: rgba(16, 25, 49, 0.96) !important;
    }

    :global(html.dark) .btn-primary {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: rgba(255, 255, 255, 0.96) !important;
    }

    :global(html.dark) .btn-primary:hover,
    :global(html.dark) .btn-primary:focus,
    :global(html.dark) .btn-primary:active {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.03) 100%);
        border-color: rgba(255, 255, 255, 0.25);
        color: rgba(255, 255, 255, 1) !important;
    }

    :global(html.dark) .btn-primary:visited {
        color: rgba(255, 255, 255, 0.96) !important;
    }

    :global(html.dark) .btn-ghost {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: rgba(255, 255, 255, 0.96) !important;
    }

    :global(html.dark) .btn-ghost:hover,
    :global(html.dark) .btn-ghost:focus,
    :global(html.dark) .btn-ghost:active {
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.03) 100%);
        border-color: rgba(255, 255, 255, 0.25);
        color: rgba(255, 255, 255, 1) !important;
    }

    :global(html.dark) .btn-ghost:visited,
    :global(html.dark) .btn-ghost:link {
        color: rgba(255, 255, 255, 0.96) !important;
    }

    .hero-visual {
        display: none;
    }

    .hero-visual::before {
        content: none;
    }

    .services-grid {
        margin-top: 6rem;
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 1.55rem;
    }

    .services {
        background: transparent;
        border-radius: 0;
        padding: 2.2rem 1rem 2.5rem;
        border: 0;
        margin-top: 12rem;
        width: 100%;
        box-sizing: border-box;
    }

    .services > .eyebrow,
    .services > h2 {
        text-align: center;
        margin-inline: auto;
        transform: translateY(-0.55rem);
    }

    .services > .eyebrow {
        color: rgba(32, 46, 78, 0.78);
        margin-bottom: 0.8rem;
    }

    .services > h2 {
        color: rgba(19, 28, 52, 0.98);
        max-width: 12ch;
        font-size: clamp(2.2rem, 4.3vw, 3.25rem);
    }

    .service-card {
        position: relative;
        overflow: hidden;
        isolation: isolate;
        transform-style: preserve-3d;
        border: 1px solid rgba(70, 103, 190, 0.28);
        border-radius: 1rem;
        background:
            radial-gradient(90% 62% at 18% 2%, rgba(255, 255, 255, 0.7) 0%, rgba(255, 255, 255, 0) 55%),
            linear-gradient(135deg, #ffffff 0%, #f6f9ff 52%, #eef3ff 100%);
        min-height: 196px;
        display: grid;
        align-items: start;
        gap: 0.58rem;
        padding: 1.22rem 1.28rem 1.14rem;
        box-shadow:
            0 0 0 1px rgba(255, 255, 255, 0.5) inset,
            0 0 14px rgba(69, 109, 236, 0.2),
            0 10px 24px rgba(26, 44, 94, 0.14);
        transform-style: preserve-3d;
        transition: transform 220ms ease;
        will-change: transform;
        text-align: center;
        justify-items: center;
    }

    .service-card[data-reveal] {
        opacity: 0;
        transform: translate3d(-14px, 28px, 0) scale(0.992);
        filter: none;
        transition: none;
    }

    .service-card[data-reveal].is-visible {
        opacity: 0;
        transform: translate3d(-14px, 28px, 0) scale(0.992);
        filter: none;
        animation: serviceCardReveal 2300ms cubic-bezier(0.22, 1, 0.36, 1) forwards;
        will-change: transform, opacity;
    }

    .tilt-layer {
        transform-style: preserve-3d;
        transition: transform 220ms ease;
        will-change: transform;
    }

    .service-card::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: inherit;
        pointer-events: none;
        box-shadow: 0 0 0 1px rgba(59, 108, 244, 0.22) inset;
    }

    .service-card::after {
        content: "";
        position: absolute;
        left: 0;
        right: 0;
        bottom: 0;
        height: 20px;
        border-radius: 0 0 1rem 1rem;
        background:
            radial-gradient(circle at 1px 1px, rgba(58, 118, 255, 0.3) 1px, transparent 1.2px),
            linear-gradient(180deg, rgba(34, 76, 224, 0) 0%, rgba(26, 86, 255, 0.2) 100%);
        background-size: 3px 3px, 100% 100%;
        background-repeat: repeat, no-repeat;
        opacity: 0.62;
        pointer-events: none;
    }

    .service-icon {
        width: 28px;
        height: 28px;
        border-radius: 0.45rem;
        display: grid;
        place-items: center;
        background: linear-gradient(140deg, #1d49db, #3058f6);
        border: 1px solid rgba(123, 153, 255, 0.92);
        box-shadow: 0 0 16px rgba(82, 122, 255, 0.5);
        position: relative;
        color: rgba(244, 248, 255, 0.96);
    }

    .service-icon::after {
        content: "";
        position: absolute;
        inset: -10px;
        border-radius: 0.7rem;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.28), rgba(255, 255, 255, 0));
        filter: blur(3px);
        z-index: -1;
    }

    .service-icon svg {
        width: 18px;
        height: 18px;
    }

    .service-card h3 {
        margin: 0.7rem 0 0;
        font-size: clamp(1.75rem, 1.9vw, 2.05rem);
        line-height: 1.08;
        font-weight: 600;
        color: rgba(16, 24, 48, 0.96);
        letter-spacing: -0.015em;
    }

    .service-card p {
        margin: 0;
        font-size: 0.92rem;
        line-height: 1.4;
        color: rgba(33, 48, 84, 0.9);
        max-width: 29ch;
    }

    .process {
        width: 100vw;
        margin-left: calc(50% - 50vw);
        margin-right: calc(50% - 50vw);
        padding: 4.2rem clamp(1.1rem, 3vw, 2.2rem) 4.8rem;
        background: transparent;
        display: none;
    }

    .process > h2 {
        text-align: center;
        margin-inline: auto;
        color: #101a35;
        width: min(1020px, calc(100% - 2.2rem));
        font-size: clamp(2.2rem, 4.2vw, 3.8rem);
        letter-spacing: -0.02em;
        line-height: 1.04;
    }

    .process-grid {
        margin-top: 2.35rem;
        width: min(1080px, calc(100% - 2.2rem));
        margin-inline: auto;
        --process-gap: 1.35rem;
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: 1.35rem;
        position: relative;
        align-items: start;
        isolation: isolate;
    }

    .step-card {
        position: relative;
        z-index: 1;
        text-align: center;
        padding-top: 0.2rem;
        opacity: 0;
        transform: translate3d(0, 26px, 0);
        transition:
            opacity 1400ms cubic-bezier(0.22, 1, 0.36, 1),
            transform 1900ms cubic-bezier(0.22, 1, 0.36, 1);
        transition-delay: calc(320ms + var(--stage-delay, 0ms));
    }

    .step-card::after {
        content: "";
        position: absolute;
        top: 47px;
        left: calc(50% + 47px);
        width: calc(100% + var(--process-gap, 1.35rem) - 94px);
        height: 2px;
        background: linear-gradient(90deg, rgba(62, 101, 207, 0.6) 0%, rgba(51, 90, 201, 0.82) 100%);
        transform: scaleX(0);
        transform-origin: left center;
        transition: transform 1350ms cubic-bezier(0.22, 1, 0.36, 1);
        transition-delay: calc(760ms + var(--stage-delay, 0ms));
        z-index: 3;
        pointer-events: none;
    }

    .step-card:last-child::after {
        display: none;
    }

    .process.is-line-active .step-card {
        opacity: 1;
        transform: translate3d(0, 0, 0);
    }

    .process.is-line-active .step-card::after {
        transform: scaleX(1);
    }

    .step-dot {
        width: 94px;
        height: 94px;
        border-radius: 999px;
        border: 3px solid #2f52ff;
        box-shadow: 0 0 0 2px rgba(36, 79, 255, 0.12), 0 0 18px rgba(37, 86, 255, 0.24);
        background: #f9fbff;
        margin: 0 auto;
        display: grid;
        place-items: center;
        color: rgba(24, 42, 88, 0.96);
    }

    .step-icon {
        width: 46px;
        height: 46px;
        animation: iconFloat 2.8s ease-in-out infinite;
        transform-origin: center;
    }

    .icon-discovery {
        animation: iconSearch 3.2s ease-in-out infinite;
    }

    .icon-development {
        animation: iconPulse 2.4s ease-in-out infinite;
    }

    .icon-testing {
        animation: iconCheck 2.6s ease-in-out infinite;
    }

    .icon-support {
        animation: iconSpin 3.8s linear infinite;
    }

    .icon-development .dev-left,
    .icon-development .dev-right {
        animation: devBracket 1.9s ease-in-out infinite;
    }

    .icon-development .dev-right {
        animation-delay: 120ms;
    }

    .icon-testing .check-mark {
        stroke-dasharray: 12;
        stroke-dashoffset: 12;
        animation: drawCheck 2.2s ease-in-out infinite;
    }

    @keyframes iconFloat {
        0%,
        100% {
            transform: translateY(0px);
            opacity: 0.92;
        }
        50% {
            transform: translateY(-1.8px);
            opacity: 1;
        }
    }

    @keyframes iconPulse {
        0%,
        100% {
            transform: scale(0.94);
        }
        50% {
            transform: scale(1.08);
        }
    }

    @keyframes iconCheck {
        0%,
        100% {
            opacity: 0.82;
        }
        50% {
            opacity: 1;
        }
    }

    @keyframes iconSpin {
        0% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(360deg);
        }
    }

    @keyframes iconSearch {
        0%,
        100% {
            transform: translateY(0) rotate(0deg);
        }
        35% {
            transform: translateY(-1px) rotate(-8deg);
        }
        70% {
            transform: translateY(1px) rotate(8deg);
        }
    }

    @keyframes devBracket {
        0%,
        100% {
            transform: translateX(0);
        }
        50% {
            transform: translateX(1.6px);
        }
    }

    @keyframes drawCheck {
        0%,
        15% {
            stroke-dashoffset: 12;
            opacity: 0.75;
        }
        45%,
        70% {
            stroke-dashoffset: 0;
            opacity: 1;
        }
        100% {
            stroke-dashoffset: 0;
            opacity: 0.9;
        }
    }

    @keyframes serviceCardReveal {
        0% {
            opacity: 0;
            transform: translate3d(-14px, 28px, 0) scale(0.992);
        }
        58% {
            opacity: 0.84;
            transform: translate3d(-4px, 10px, 0) scale(0.997);
        }
        100% {
            opacity: 1;
            transform: translate3d(0, 0, 0) scale(1);
        }
    }

    .step-number {
        margin: 1.1rem 0 0.85rem;
        font-size: clamp(2.2rem, 3.35vw, 3.45rem);
        font-weight: 500;
        letter-spacing: 0.02em;
        color: rgba(16, 27, 56, 0.96);
        line-height: 1;
    }

    .step-card h3 {
        margin: 0;
        font-size: clamp(1.08rem, 1.55vw, 1.9rem);
        color: rgba(21, 32, 62, 0.98);
        line-height: 1.2;
    }

    .step-card p {
        margin: 1.05rem auto 0;
        font-size: clamp(0.9rem, 1vw, 1.03rem);
        line-height: 1.65;
        color: rgba(35, 51, 86, 0.9);
        max-width: 32ch;
    }

    .portfolio {
        width: 100vw;
        margin-left: calc(50% - 50vw);
        margin-right: calc(50% - 50vw);
        background: transparent;
        padding: 0 0 clamp(1.6rem, 2.2vw, 2.2rem);
        margin-top: -2rem;
    }

    .portfolio > .eyebrow,
    .portfolio > h2 {
        text-align: center;
        margin-inline: auto;
        width: min(1120px, calc(100% - 2.6rem));
        color: rgba(20, 31, 61, 0.95);
    }

    .portfolio > .eyebrow {
        color: rgba(52, 69, 108, 0.78);
    }

    .portfolio > h2 {
        max-width: 16ch;
        margin-top: 0.1rem;
        font-size: clamp(2rem, 3.5vw, 3.1rem);
        line-height: 1.05;
    }

    .portfolio-row {
        margin-top: 1.8rem;
        width: min(1120px, calc(100% - 2.6rem));
        margin-inline: auto;
        display: flex;
        gap: clamp(0.75rem, 1vw, 1rem);
        overflow-x: auto;
        scroll-snap-type: x mandatory;
        scroll-padding-inline: 0.4rem;
        scroll-behavior: auto;
        overscroll-behavior-x: contain;
        touch-action: pan-x pinch-zoom;
        -webkit-overflow-scrolling: touch;
        padding: 0.2rem 0 0.9rem;
        scrollbar-width: none;
        -ms-overflow-style: none;
        cursor: grab;
    }

    .portfolio-row.is-dragging {
        cursor: grabbing;
        user-select: none;
        scroll-snap-type: none;
    }

    .portfolio-row.is-momentum {
        scroll-snap-type: x proximity;
    }

    .portfolio-row::-webkit-scrollbar {
        display: none;
    }

    .project-card {
        scroll-snap-align: start;
        scroll-snap-stop: always;
        flex: 0 0 clamp(220px, 24vw, 300px);
        width: clamp(220px, 24vw, 300px);
        background: transparent;
        border: 0;
        min-height: unset;
        display: grid;
        gap: 0.78rem;
        text-align: center;
        justify-items: center;
    }

    .project-media {
        width: 100%;
        border-radius: 1.3rem;
        overflow: hidden;
        aspect-ratio: 1 / 1;
        background: #0f1117;
        box-shadow: 0 14px 34px rgba(0, 0, 0, 0.42);
    }

    .project-card img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 360ms ease;
        -webkit-user-drag: none;
        user-select: none;
        pointer-events: none;
    }

    .project-card:hover img {
        transform: scale(1.03);
    }

    .project-card h3 {
        margin: 0;
        width: 100%;
        font-size: 1.06rem;
        letter-spacing: 0.02em;
        text-transform: uppercase;
        color: rgba(18, 29, 56, 0.98);
    }

    .project-card p {
        margin: 0;
        width: 100%;
        font-size: 0.95rem;
        color: rgba(51, 67, 106, 0.84);
        line-height: 1.35;
        max-width: 100%;
    }

    .project-card--empty .project-media {
        border: 1px dashed rgba(112, 136, 187, 0.42);
        background: linear-gradient(145deg, rgba(244, 248, 255, 0.95), rgba(234, 240, 252, 0.95));
        box-shadow: none;
    }

    .portfolio-cta {
        margin-top: 2.5rem;
        display: flex;
        justify-content: center;
        width: min(1120px, calc(100% - 2.6rem));
        margin-inline: auto;
    }

    .team {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
        align-items: center;
    }

    .team-copy {
        text-align: center;
        justify-items: center;
        display: grid;
    }

    .team-copy .eyebrow {
        color: rgba(34, 48, 82, 0.8);
    }

    .team-copy h2 {
        color: rgba(16, 27, 56, 0.98);
    }

    .team-copy p {
        color: rgba(40, 56, 93, 0.9);
    }

    /* Video CTA Banner */
    .video-cta-banner {
        width: 100vw;
        margin-left: calc(50% - 50vw);
        margin-right: calc(50% - 50vw);
        margin-top: 4rem;
        margin-bottom: 4rem;
    }

    .video-cta-container {
        position: relative;
        width: min(1120px, calc(100% - 2.6rem));
        margin: 0 auto;
        border-radius: 2rem;
        overflow: hidden;
        min-height: 450px;
        display: flex;
        align-items: center;
        justify-content: flex-start;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        background: #2a2a2a;
    }

    .video-background {
        position: absolute;
        inset: 0;
        z-index: 0;
    }

    .video-element {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
        opacity: 1;
    }

    .video-cta-content {
        position: relative;
        z-index: 10;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 1.5rem;
        padding: 4rem clamp(2rem, 6vw, 5rem);
        text-align: center;
        max-width: 600px;
        margin: 0;
        width: fit-content;
    }

    .video-cta-title {
        margin: 0;
        font-size: clamp(1.5rem, 3.5vw, 2rem);
        line-height: 1.25;
        font-weight: 700;
        color: #ffffff;
        text-shadow: 
            0 0 6px rgba(0, 0, 0, 0.9),
            0 0 10px rgba(0, 0, 0, 0.7),
            -1.5px -1.5px 0 rgba(0, 0, 0, 0.8),
            1.5px -1.5px 0 rgba(0, 0, 0, 0.8),
            -1.5px 1.5px 0 rgba(0, 0, 0, 0.8),
            1.5px 1.5px 0 rgba(0, 0, 0, 0.8),
            -2px 0 0 rgba(0, 0, 0, 0.7),
            2px 0 0 rgba(0, 0, 0, 0.7),
            0 -2px 0 rgba(0, 0, 0, 0.7),
            0 2px 0 rgba(0, 0, 0, 0.7);
        max-width: 20ch;
        margin-bottom: 0.5rem;
    }

    .video-cta-btn {
        font-size: 1rem;
        padding: 1rem 2rem;
        background: rgba(255, 255, 255, 0.95);
        box-shadow: 
            0 10px 30px -10px rgba(0, 0, 0, 0.2),
            inset 0 1px 1px rgba(255, 255, 255, 0.5);
        border: 1px solid rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        color: rgba(16, 25, 49, 0.96) !important;
        align-self: center;
        margin-top: 0;
    }

    .video-cta-btn:hover {
        background: rgba(255, 255, 255, 1);
        box-shadow: 
            0 20px 40px -10px rgba(0, 0, 0, 0.3),
            inset 0 1px 1px rgba(255, 255, 255, 0.8);
        border-color: rgba(255, 255, 255, 0.5);
        color: rgba(16, 25, 49, 1) !important;
    }

    @media (max-width: 768px) {
        .video-cta-container {
            min-height: 320px;
            border-radius: 1.2rem;
            width: calc(100% - 2rem);
        }

        .video-cta-content {
            padding: 2.5rem 1.5rem;
        }

        .video-cta-title {
            max-width: 100%;
            font-size: clamp(1.3rem, 5vw, 1.8rem);
        }

        .video-cta-banner {
            margin-top: 2rem;
            margin-bottom: 2rem;
        }
    }

    :global(html:not(.dark)) .team-copy .eyebrow {
        color: #25365f !important;
    }

    :global(html:not(.dark)) .team-copy h2 {
        color: #111c3a !important;
    }

    :global(html:not(.dark)) .team-copy p {
        color: #27375d !important;
    }

    :global(html.dark) .eyebrow {
        color: rgba(229, 236, 255, 0.7);
    }

    :global(html.dark) .subtitle,
    :global(html.dark) .hero-meta,
    :global(html.dark) .step-card p,
    :global(html.dark) .project-card p,
    :global(html.dark) .service-card p {
        color: rgba(229, 236, 255, 0.76);
    }


    :global(html.dark) .service-card,
    :global(html.dark) .project-card {
        border-color: rgba(90, 104, 140, 0.28);
        background: linear-gradient(180deg, rgba(16, 18, 24, 0.94), rgba(11, 13, 18, 0.86));
        box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.04) inset;
    }

    :global(html.dark) .services {
        background: transparent;
    }

    :global(html.dark) .services > .eyebrow {
        color: rgba(230, 236, 255, 0.78);
    }

    :global(html.dark) .services > h2 {
        color: rgba(247, 249, 255, 0.98);
    }

    :global(html.dark) .service-card {
        border: 1px solid rgba(45, 103, 255, 0.92);
        background:
            radial-gradient(90% 62% at 18% 2%, rgba(255, 255, 255, 0.045) 0%, rgba(255, 255, 255, 0) 55%),
            linear-gradient(135deg, #171a22 0%, #10131a 52%, #090b10 100%);
        box-shadow:
            0 0 0 1px rgba(255, 255, 255, 0.04) inset,
            0 0 22px rgba(24, 75, 255, 0.34),
            0 14px 28px rgba(5, 10, 26, 0.68);
    }

    :global(html.dark) .service-card::before {
        box-shadow: 0 0 0 1px rgba(59, 108, 244, 0.46) inset;
    }

    :global(html.dark) .service-card::after {
        background:
            radial-gradient(circle at 1px 1px, rgba(58, 118, 255, 0.58) 1px, transparent 1.2px),
            linear-gradient(180deg, rgba(34, 76, 224, 0) 0%, rgba(26, 86, 255, 0.42) 100%);
    }

    :global(html.dark) .service-card h3 {
        color: rgba(246, 248, 255, 0.96);
    }

    :global(html.dark) .service-card p {
        color: rgba(233, 239, 255, 0.9);
    }

    :global(html.dark) .process {
        background: transparent;
    }

    :global(html.dark) .process > h2 {
        color: #f5f7ff;
    }

    :global(html.dark) .step-card::after {
        background: linear-gradient(90deg, rgba(232, 241, 255, 0.95) 0%, rgba(238, 245, 255, 1) 100%);
    }

    :global(html.dark) .step-dot {
        box-shadow: 0 0 0 2px rgba(36, 79, 255, 0.16), 0 0 20px rgba(37, 86, 255, 0.36);
        background: #04070d;
        color: rgba(226, 236, 255, 0.96);
    }

    :global(html.dark) .step-number {
        color: rgba(244, 248, 255, 0.96);
    }

    :global(html.dark) .step-card h3 {
        color: rgba(248, 251, 255, 0.98);
    }

    :global(html.dark) .step-card p {
        color: rgba(241, 246, 255, 0.9);
    }

    :global(html.dark) .portfolio {
        background: transparent;
    }

    :global(html.dark) .portfolio > .eyebrow,
    :global(html.dark) .portfolio > h2 {
        color: rgba(243, 246, 255, 0.95);
    }

    :global(html.dark) .portfolio > .eyebrow {
        color: rgba(202, 209, 225, 0.78);
    }

    :global(html.dark) .project-card h3 {
        color: rgba(247, 250, 255, 0.98);
    }

    :global(html.dark) .project-card p {
        color: rgba(199, 207, 224, 0.84);
    }

    :global(html.dark) .project-card--empty .project-media {
        border: 1px dashed rgba(185, 194, 214, 0.32);
        background: linear-gradient(145deg, rgba(20, 22, 28, 0.9), rgba(12, 14, 19, 0.9));
    }

    :global(html.dark) .team {
        background: transparent;
        border-radius: 0;
        padding: 0;
    }

    :global(html.dark) .team-copy .eyebrow {
        color: rgba(229, 236, 255, 0.7);
    }

    :global(html.dark) .team-copy h2 {
        color: rgba(247, 250, 255, 0.98);
    }

    :global(html.dark) .team-copy p {
        color: rgba(229, 236, 255, 0.82);
    }

    :global(html.dark) .home {
        gap: 1.8rem;
    }

    .stacking-cards-section {
        width: 100%;
        margin-left: 0;
        margin-right: 0;
        padding: 0;
        margin-top: 4rem;
        margin-bottom: 0;
        overflow: visible;
        position: relative;
    }
    
    .stacking-cards-eyebrow {
        text-align: center;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        font-size: 0.72rem;
        color: rgba(23, 30, 48, 0.72);
        margin-bottom: 0.9rem;
    }
    
    :global(html.dark) .stacking-cards-eyebrow {
        color: rgba(255, 255, 255, 0.85);
    }
    
    .stacking-cards-title {
        text-align: center;
        font-size: clamp(2.2rem, 4.3vw, 3.25rem);
        color: rgba(19, 28, 52, 0.98);
        margin-bottom: -10rem;
        margin-inline: auto;
        max-width: 20ch;
    }
    
    :global(html.dark) .stacking-cards-title {
        color: #ffffff;
    }
    
    .stacking-cards-section :global(main) {
        min-height: 100vh;
    }

    @media (max-width: 960px) {
        .hero,
        .team {
            grid-template-columns: 1fr;
        }

        .hero {
            padding-top: 0;
            margin-top: -0.4rem;
        }

        .hero-visual {
            min-height: 420px;
            margin-top: 0.8rem;
        }

        .process-grid {
            --process-gap: 1.25rem;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 1.6rem 1.25rem;
        }

        .step-dot {
            width: 84px;
            height: 84px;
        }

        .step-card::after {
            top: 42px;
            left: calc(50% + 42px);
            width: calc(100% + var(--process-gap, 1.25rem) - 84px);
        }

    }

    @media (max-width: 760px) {
        .home {
            padding-top: 1.2rem;
            gap: 2.5rem;
            width: calc(100% - 1.6rem);
        }

        .hero-intro {
            padding-top: 4rem;
            padding-bottom: 0;
        }

        .hero-intro-content {
            width: 100%;
            gap: 1.2rem;
        }

        .hero-title {
            font-size: clamp(2rem, 10vw, 3.2rem);
        }

        .subtitle {
            font-size: 0.95rem;
            max-width: 100%;
        }

        .hero-visual {
            min-height: 300px;
        }

        .services {
            margin-top: 4rem;
            padding: 1.5rem 0.5rem 2rem;
        }

        .services > h2 {
            max-width: 100%;
            font-size: clamp(1.6rem, 6vw, 2.2rem);
        }

        .services-grid {
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .portfolio {
            padding: 0 0 1.5rem;
            margin-top: 0;
        }

        .portfolio > h2 {
            max-width: 100%;
            font-size: clamp(1.5rem, 6vw, 2rem);
        }

        .portfolio-row {
            width: calc(100% - 1.6rem);
        }

        .project-card {
            flex: 0 0 clamp(200px, 70vw, 280px);
            width: clamp(200px, 70vw, 280px);
        }

        .process-grid {
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        .step-card::after {
            display: none;
        }

        .step-dot {
            width: 78px;
            height: 78px;
        }

        .team {
            padding: 0 0.5rem;
        }

        .team-copy h2 {
            max-width: 100%;
            font-size: clamp(1.5rem, 6vw, 2rem);
        }

        .stacking-cards-section {
            margin-top: 2rem;
        }

        .stacking-cards-title {
            font-size: clamp(1.6rem, 6vw, 2.5rem);
            margin-bottom: -3rem;
        }
    }

    @media (max-width: 480px) {
        .home {
            width: calc(100% - 1.2rem);
            gap: 2rem;
        }

        .hero-title {
            font-size: clamp(1.8rem, 9vw, 2.8rem);
        }

        .actions {
            flex-direction: column;
            width: 100%;
            align-items: center;
        }

        .btn {
            width: 100%;
            max-width: 280px;
            text-align: center;
            justify-content: center;
        }

        h2 {
            font-size: clamp(1.4rem, 5.5vw, 2rem);
            max-width: 100%;
        }

        .eyebrow {
            font-size: 0.65rem;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .service-card[data-reveal],
        .service-card[data-reveal].is-visible {
            animation: none !important;
            opacity: 1;
            transform: none;
            filter: none;
        }
    }
</style>
