---
import Layout from "../../layouts/Layout.astro";
import YouCanScroll from "../../components/YouCanScroll.astro";
import InteractiveNeuralVortex from "../../components/ui/interactive-neural-vortex-background";
import { SITE_DESCRIPTION } from "../../consts";
import { getLangFromUrl } from "../../i18n/translate";

const lang = getLangFromUrl(Astro.url);

const copy =
    lang === "en"
        ? {
              badge: "Technology studio",
              heroTitle: "Need an app, website, or system for your company?",
              heroText: "We build digital products with strategy, design, and engineering to help your business scale.",
              heroPrimary: "Start a project",
              heroSecondary: "View services",
              heroMetaA: "Consulting and Product",
              heroMetaB: "In-house software team",
              heroGapTitle: "",
              servicesTitle: "What can we build together?",
              servicesSubtitle: "Examples",
              processTitle: "How it works",
              processSubtitle: "Work process",
              portfolioTitle: "Your industry could be next",
              portfolioSubtitle: "Success stories",
              withUsSubtitle: "Solutions Squad",
              teamTitle: "Our team",
              teamText: "Product designers, engineers, and growth specialists focused on real outcomes.",
              teamAction: "View founders",
              ctaText: "Join the future of autonomous software development.",
              ctaAction: "Schedule a meeting",
              endTitle: "Ready to digitize your business?",
              endPrimary: "Let's start",
                            endSecondary: "Learn more about us",
          }
        : {
              badge: "Estudio de tecnologia",
              heroTitle: "Necesitas una app, web o sistema para tu empresa?",
              heroText: "Construimos productos digitales con estrategia, diseno y desarrollo para escalar tu negocio.",
              heroPrimary: "Iniciar proyecto",
              heroSecondary: "Ver servicios",
              heroMetaA: "Consultoria y producto",
              heroMetaB: "Equipo interno de software",
              heroGapTitle: "",
              servicesTitle: "Que podemos crear juntos?",
              servicesSubtitle: "Ejemplos",
              processTitle: "Como funciona",
              processSubtitle: "Proceso de trabajo",
              portfolioTitle: "Ya lo hicimos. Tu industria puede ser la proxima",
              portfolioSubtitle: "Casos reales",
              withUsSubtitle: "Solutions Squad",
              teamTitle: "Nuestro equipo",
              teamText: "Disenadores, developers y especialistas en crecimiento enfocados en resultados reales.",
              teamAction: "Ver founders",
              ctaText: "Sumate al futuro del desarrollo de software autonomo.",
              ctaAction: "Agenda una reunion",
              endTitle: "Listo para digitalizar tu negocio?",
              endPrimary: "Comencemos",
              endSecondary: "Conoce mas sobre nosotros",
          };

const serviceItems =
    lang === "en"
        ? [
              {
                  title: "App and web systems",
                  text: "Order, stock, shift, and customer management. Built around your workflow.",
              },
              {
                  title: "Dashboards and reports",
                  text: "Clear real-time business data for better decisions.",
              },
              {
                  title: "Automation",
                  text: "Remove repetitive tasks so your team can focus on what matters.",
              },
              {
                  title: "E-commerce",
                  text: "Online stores integrated with payment, invoicing, logistics, and stock.",
              },
              {
                  title: "Artificial intelligence",
                  text: "Chatbots, demand prediction, and data analysis with AI for your business.",
              },
              {
                  title: "Integrations",
                  text: "We connect tools like SAP, Tango, MercadoPago, WhatsApp, and your current stack.",
              },
          ]
        : [
              {
                  title: "App y sistemas web",
                  text: "Gestion de pedidos, stock, turnos y clientes. Lo que necesites, a medida.",
              },
              {
                  title: "Dashboards e informes",
                  text: "Datos de tu negocio claros y en tiempo real para tomar mejores decisiones.",
              },
              {
                  title: "Automatizacion",
                  text: "Elimina tareas manuales y repetitivas para que tu equipo se enfoque en lo importante.",
              },
              {
                  title: "E-commerce",
                  text: "Tiendas online integradas con medios de pago, facturacion, logistica y stock.",
              },
              {
                  title: "Inteligencia artificial",
                  text: "Chatbots, prediccion de demanda y analisis de datos con IA aplicada a tu negocio.",
              },
              {
                  title: "Integraciones",
                  text: "Conectamos SAP, Tango, MercadoPago, WhatsApp y lo que ya uses hoy.",
              },
          ];

const steps = [
    {
        number: "01",
        title: "Descubrimiento y analisis",
        text: "Entendemos negocio, procesos y objetivos para definir una base clara.",
    },
    {
        number: "02",
        title: "Diseno estrategico",
        text: "Prototipos y arquitectura para validar antes de desarrollar.",
    },
    {
        number: "03",
        title: "Pruebas y lanzamiento",
        text: "Construimos, iteramos y publicamos una version lista para crecer.",
    },
    {
        number: "04",
        title: "Soporte continuo",
        text: "Mejora evolutiva con datos reales de uso y negocio.",
    },
];

const projects = [
    {
        image: "/projects/hexadevs.png",
        title: "Hexadevs",
        text: "Sitio institucional con foco en conversion.",
    },
    {
        image: "/projects/todo-app.svg",
        title: "Taskflow",
        text: "Sistema de tareas y productividad para equipos.",
    },
    {
        image: "/projects/flashcards.svg",
        title: "Learning Hub",
        text: "Plataforma educativa para aprendizaje dinamico.",
    },
    {
        image: "/projects/bunny-js.svg",
        title: "Bunny JS",
        text: "Web app ligera orientada a performance y conversion.",
    },
    {
        image: "/projects/animal.svg",
        title: "Animal UI",
        text: "Landing visual con identidad de marca y narrativa clara.",
    },
];

---

<Layout title="CROBF" description={SITE_DESCRIPTION}>
    <script is:inline type="module" src="https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js"></script>
    <main class="home">
        <section class="hero" id="inicio">
            <div class="hero-copy">
                <p class="eyebrow">{copy.badge}</p>
                <h1>{copy.heroTitle}</h1>
                <p class="subtitle">{copy.heroText}</p>
                <ul class="hero-meta">
                    <li>{copy.heroMetaA}</li>
                    <li>{copy.heroMetaB}</li>
                </ul>
                <div class="actions">
                    <a href={`/${lang}/contact`} class="btn btn-primary">{copy.heroPrimary}</a>
                    <a href="#services" class="btn btn-ghost">{copy.heroSecondary}</a>
                </div>
            </div>
            <div class="hero-visual" aria-hidden="true">
                <model-viewer
                    class="hero-phone"
                    src="/models/iphone_17_pro_max.glb"
                    alt="Phone 3D model"
                    auto-rotate
                    rotation-per-second="10deg"
                    interaction-prompt="none"
                    disable-zoom
                    shadow-intensity="0.9"
                    exposure="1.1"
                ></model-viewer>
            </div>
        </section>

        <section class="scroll-frames" aria-label="Animacion por desplazamiento">
            <div class="scroll-frames-sticky">
                <canvas class="scroll-frames-canvas" aria-label="Animacion de producto"></canvas>
                <div class="frames-neural-overlay" aria-hidden="true">
                    <InteractiveNeuralVortex client:load hideHero={true} embedded={true} className="frames-neural-react" />
                </div>
                <div class="frames-loader" role="status" aria-live="polite">
                    <span class="frames-loader-label">Cargando animacion...</span>
                    <span class="frames-loader-progress">0%</span>
                </div>
            </div>
        </section>

        <section class="services" id="services">
            <p class="eyebrow">{copy.servicesSubtitle}</p>
            <h2>{copy.servicesTitle}</h2>
            <div class="services-grid">
                {
                    serviceItems.map((service) => (
                        <article class="service-card tilt-card">
                            <span class="service-icon tilt-layer" data-depth="18" aria-hidden="true"></span>
                            <h3 class="tilt-layer" data-depth="24">{service.title}</h3>
                            <p class="tilt-layer" data-depth="14">{service.text}</p>
                        </article>
                    ))
                }
            </div>
        </section>

        <section class="process">
            <p class="eyebrow">{copy.processSubtitle}</p>
            <h2>{copy.processTitle}</h2>
            <div class="process-grid">
                {
                    steps.map((step) => (
                        <article class="step-card">
                            <div class="step-dot"></div>
                            <p class="step-number">{step.number}</p>
                            <h3>{step.title}</h3>
                            <p>{step.text}</p>
                        </article>
                    ))
                }
            </div>
        </section>

        <section class="portfolio" id="casos-exito">
            <p class="eyebrow">{copy.portfolioSubtitle}</p>
            <h2>{copy.portfolioTitle}</h2>
            <div class="portfolio-row">
                {
                    projects.map((project) => (
                        <article class="project-card">
                            <img src={project.image} alt={project.title} loading="lazy" />
                            <h3>{project.title}</h3>
                            <p>{project.text}</p>
                        </article>
                    ))
                }
            </div>
        </section>

        <YouCanScroll />

        <section class="team" id="about-us">
            <div class="team-grid" aria-hidden="true">
                <span class="block one"></span>
                <span class="block two"></span>
                <span class="block three"></span>
                <span class="block four"></span>
                <span class="block five"></span>
                <span class="block six"></span>
            </div>
            <div class="team-copy">
                <p class="eyebrow">{copy.withUsSubtitle}</p>
                <h2>{copy.teamTitle}</h2>
                <p>{copy.teamText}</p>
                <a href={`/${lang}/about`} class="btn btn-primary">{copy.teamAction}</a>
            </div>
        </section>

        <section class="floating-cta">
            <p>{copy.ctaText}</p>
            <a href={`/${lang}/contact`} class="btn btn-primary">{copy.ctaAction}</a>
        </section>

        <section class="end-cta" id="contacto">
            <h2>{copy.endTitle}</h2>
            <div class="actions">
                <a href={`/${lang}/contact`} class="btn btn-ghost">{copy.endPrimary}</a>
                <a href={`/${lang}/about`} class="btn btn-primary">{copy.endSecondary}</a>
            </div>
        </section>

        <script is:inline type="module">
            const framesSection = document.querySelector(".scroll-frames");
            if (framesSection) {
                const canvas = framesSection.querySelector(".scroll-frames-canvas");
                const loader = framesSection.querySelector(".frames-loader");
                const loaderProgress = framesSection.querySelector(".frames-loader-progress");
                const neuralOverlay = framesSection.querySelector(".frames-neural-overlay");

                if (canvas instanceof HTMLCanvasElement) {
                    const ctx = canvas.getContext("2d", { alpha: true });
                    const totalFrames = 91;
                    const videoWidth = 1920;
                    const videoHeight = 1080;
                    const notebookScreen = {
                        x: 0.158,
                        y: 0.05,
                        width: 0.684,
                        height: 0.742,
                    };
                    const images = new Array(totalFrames);
                    let loadedCount = 0;
                    let rafPending = false;
                    let currentFrame = -1;
                    let currentProgress = -1;
                    let renderWidth = 0;
                    let renderHeight = 0;

                    const updateLoader = () => {
                        const percent = Math.round((loadedCount / totalFrames) * 100);
                        if (loaderProgress) loaderProgress.textContent = `${percent}%`;
                        if (loader && loadedCount >= totalFrames) loader.classList.add("is-complete");
                    };

                    const resizeCanvas = () => {
                        const viewportW = window.innerWidth;
                        const viewportH = window.innerHeight;

                        renderWidth = viewportW;
                        renderHeight = Math.round(viewportH * 0.96);

                        const dpr = Math.max(1, window.devicePixelRatio || 1);
                        canvas.style.width = `${renderWidth}px`;
                        canvas.style.height = `${renderHeight}px`;
                        canvas.width = Math.round(renderWidth * dpr);
                        canvas.height = Math.round(renderHeight * dpr);

                        if (!ctx) return;
                        ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
                        if (currentFrame >= 0 && images[currentFrame]) {
                            drawFrame(currentFrame, currentProgress < 0 ? 0 : currentProgress);
                        }
                    };

                    const drawFrame = (frameIndex, progress) => {
                        if (!ctx || !images[frameIndex]) return;

                        const containScale = Math.min(renderWidth / videoWidth, renderHeight / videoHeight);
                        const coverScale = Math.max(renderWidth / videoWidth, renderHeight / videoHeight) * 1.02;
                        const zoomProgress = Math.min(1, Math.max(0, (progress - 0.12) / 0.56));
                        const scale = containScale + (coverScale - containScale) * zoomProgress;

                        const frameDrawWidth = videoWidth * scale;
                        const frameDrawHeight = videoHeight * scale;
                        const offsetX = (renderWidth - frameDrawWidth) / 2;
                        const offsetY = (renderHeight - frameDrawHeight) / 2;

                        ctx.clearRect(0, 0, renderWidth, renderHeight);
                        ctx.drawImage(images[frameIndex], offsetX, offsetY, frameDrawWidth, frameDrawHeight);

                        if (neuralOverlay instanceof HTMLElement) {
                            // Coordinates normalized to the source video, then projected with the same zoom/offset as the notebook.
                            const screenX = offsetX + notebookScreen.x * frameDrawWidth;
                            const screenY = offsetY + notebookScreen.y * frameDrawHeight;
                            const screenW = notebookScreen.width * frameDrawWidth;
                            const screenH = notebookScreen.height * frameDrawHeight;
                            const openProgress = Math.min(1, Math.max(0, (progress - 0.08) / 0.48));
                            const reveal = Math.pow(openProgress, 1.2);

                            neuralOverlay.style.left = `${screenX}px`;
                            neuralOverlay.style.top = `${screenY}px`;
                            neuralOverlay.style.width = `${screenW}px`;
                            neuralOverlay.style.height = `${screenH}px`;
                            neuralOverlay.style.opacity = String(reveal * 0.98);
                            neuralOverlay.style.transform = `translateZ(0) scale(${0.94 + reveal * 0.08})`;
                            neuralOverlay.style.borderRadius = `${Math.max(8, screenH * 0.025)}px`;
                        }

                        currentFrame = frameIndex;
                        currentProgress = progress;
                    };

                    const requestDraw = (frameIndex, progress) => {
                        if (rafPending) return;
                        if (frameIndex === currentFrame && Math.abs(progress - currentProgress) < 0.002) return;
                        rafPending = true;
                        requestAnimationFrame(() => {
                            drawFrame(frameIndex, progress);
                            rafPending = false;
                        });
                    };

                    const onScroll = () => {
                        const rect = framesSection.getBoundingClientRect();
                        const scrollRange = framesSection.offsetHeight - window.innerHeight;
                        if (scrollRange <= 0) return;

                        const traveled = Math.min(scrollRange, Math.max(0, -rect.top));
                        const progress = traveled / scrollRange;
                        const targetFrame = Math.min(totalFrames - 1, Math.floor(progress * (totalFrames - 1)));
                        requestDraw(targetFrame, progress);
                    };

                    const preloadPromises = [];
                    for (let i = 1; i <= totalFrames; i += 1) {
                        const img = new Image();
                        img.decoding = "async";
                        images[i - 1] = img;

                        preloadPromises.push(
                            new Promise((resolve) => {
                                img.onload = () => {
                                    loadedCount += 1;
                                    updateLoader();
                                    resolve(true);
                                };
                                img.onerror = () => {
                                    loadedCount += 1;
                                    updateLoader();
                                    resolve(false);
                                };
                            })
                        );

                        img.src = `/videos/frames/frame_${String(i).padStart(4, "0")}.webp`;
                    }

                    resizeCanvas();
                    window.addEventListener("resize", resizeCanvas, { passive: true });
                    window.addEventListener("scroll", onScroll, { passive: true });

                    Promise.all(preloadPromises).then(() => {
                        drawFrame(0, 0);
                        onScroll();
                    });
                }
            }
        </script>

        <script is:inline>
            const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

            if (!reduceMotion) {
                document.querySelectorAll(".tilt-card").forEach((card) => {
                    const layers = card.querySelectorAll(".tilt-layer");
                    const maxRotate = 7;

                    const reset = () => {
                        card.style.transform = "perspective(900px) rotateX(0deg) rotateY(0deg) translateY(0px)";
                        layers.forEach((layer) => {
                            const depth = Number(layer.getAttribute("data-depth") || 0);
                            layer.style.transform = `translateZ(${depth}px)`;
                        });
                    };

                    reset();

                    card.addEventListener("mousemove", (event) => {
                        const rect = card.getBoundingClientRect();
                        const px = (event.clientX - rect.left) / rect.width - 0.5;
                        const py = (event.clientY - rect.top) / rect.height - 0.5;

                        const ry = px * maxRotate * 2;
                        const rx = -py * maxRotate * 2;

                        card.style.transform = `perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-2px)`;

                        layers.forEach((layer) => {
                            const depth = Number(layer.getAttribute("data-depth") || 0);
                            const lx = -px * depth * 0.5;
                            const ly = -py * depth * 0.5;
                            layer.style.transform = `translate3d(${lx}px, ${ly}px, ${depth}px)`;
                        });
                    });

                    card.addEventListener("mouseleave", reset);
                    card.addEventListener("blur", reset, true);
                });
            }
        </script>
    </main>
</Layout>

<style>
    .home {
        width: min(1120px, calc(100% - 2.2rem));
        padding: 1.2rem 0 5rem;
        display: grid;
        gap: 5.4rem;
    }

    :global(body) {
        overflow-x: hidden;
    }

    .eyebrow {
        margin: 0 0 0.9rem;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        font-size: 0.72rem;
        color: rgba(23, 30, 48, 0.72);
    }

    .hero {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.2rem;
        align-items: center;
        min-height: 58svh;
        padding-top: 1.4rem;
    }

    .hero-phone {
        position: relative;
        z-index: 3;
        width: clamp(230px, 33vw, 340px);
        height: clamp(390px, 54vw, 560px);
        flex: 0 0 auto;
        background: transparent;
        background-color: transparent;
        --poster-color: transparent;
        filter: drop-shadow(0 24px 36px rgba(4, 12, 34, 0.5));
    }

    h1 {
        font-size: clamp(2rem, 4.6vw, 4.2rem);
        line-height: 1.02;
        margin: 0;
        max-width: 14ch;
    }

    h2 {
        font-size: clamp(1.7rem, 3vw, 2.8rem);
        margin: 0;
        max-width: 20ch;
        line-height: 1.1;
    }

    .subtitle {
        color: rgba(23, 30, 48, 0.78);
        max-width: 52ch;
        margin: 1rem 0 0;
    }

    .hero-meta {
        margin: 0.95rem 0 0;
        padding: 0;
        list-style: none;
        display: grid;
        gap: 0.2rem;
        font-size: 0.72rem;
        color: rgba(31, 42, 72, 0.72);
    }

    .hero-meta li::before {
        content: "+ ";
        color: #4d6eff;
        font-weight: 700;
    }

    .actions {
        margin-top: 1.8rem;
        display: flex;
        flex-wrap: wrap;
        gap: 0.7rem;
    }

    .btn {
        padding: 0.72rem 1.2rem;
        border-radius: 999px;
        font-size: 0.86rem;
        letter-spacing: 0.02em;
        text-decoration: none;
        border: 1px solid transparent;
        transition: transform 140ms ease, box-shadow 140ms ease;
    }

    .btn:hover {
        transform: translateY(-1px);
    }

    .btn-primary {
        background: linear-gradient(120deg, #1f34f3, #4c6aff);
        color: #f6f8ff;
        box-shadow: 0 10px 30px rgba(41, 76, 255, 0.25);
    }

    .btn-ghost {
        border-color: rgba(41, 76, 255, 0.4);
        color: #263575;
        background: rgba(255, 255, 255, 0.68);
    }

    .hero-visual {
        position: relative;
        min-height: 520px;
        display: grid;
        place-items: center;
        border-radius: 1.35rem;
        overflow: visible;
        border: none;
        background: transparent;
        box-shadow: none;
    }

    .hero-visual::before {
        content: none;
    }

    .scroll-frames {
        height: 600vh;
        position: relative;
        margin-top: -0.3rem;
        width: 100vw;
        margin-left: calc(50% - 50vw);
        margin-right: calc(50% - 50vw);
        overflow-x: clip;
    }

    .scroll-frames-sticky {
        position: sticky;
        top: 0;
        min-height: 100svh;
        display: grid;
        place-items: center;
        border-radius: 0;
        background: transparent;
        border: none;
        overflow: visible;
        overflow-x: clip;
    }

    .scroll-frames-canvas {
        display: block;
        width: 100vw;
        height: 96svh;
        border-radius: 0;
        box-shadow: none;
    }

    .frames-neural-overlay {
        position: absolute;
        left: 0;
        top: 0;
        width: 0;
        height: 0;
        border-radius: 1.1rem;
        overflow: hidden;
        pointer-events: none;
        opacity: 0;
        z-index: 2;
        mix-blend-mode: normal;
        filter: saturate(1.12) contrast(1.08);
        transform-origin: center center;
        will-change: left, top, width, height, opacity, transform;
        transition: opacity 120ms linear;
    }

    .frames-neural-react {
        width: 100%;
        height: 100%;
    }

    .frames-loader {
        position: absolute;
        left: 50%;
        bottom: 6.5vh;
        transform: translateX(-50%);
        display: inline-flex;
        align-items: center;
        gap: 0.6rem;
        font-size: 0.78rem;
        padding: 0.45rem 0.75rem;
        border-radius: 999px;
        border: 1px solid rgba(85, 112, 184, 0.28);
        color: rgba(20, 32, 61, 0.9);
        background: rgba(255, 255, 255, 0.78);
        backdrop-filter: blur(7px);
        transition: opacity 220ms ease;
    }

    .frames-loader.is-complete {
        opacity: 0;
        pointer-events: none;
    }

    .services-grid {
        margin-top: 1.6rem;
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 1.55rem;
    }

    .services {
        background: linear-gradient(180deg, #05070d 0%, #070a12 100%);
        border-radius: 1rem;
        padding: 2.2rem 1.7rem 2.5rem;
    }

    .services > .eyebrow,
    .services > h2 {
        text-align: center;
        margin-inline: auto;
    }

    .services > .eyebrow {
        color: rgba(230, 236, 255, 0.78);
        margin-bottom: 0.8rem;
    }

    .services > h2 {
        color: rgba(247, 249, 255, 0.98);
        max-width: 12ch;
        font-size: clamp(2.2rem, 4.3vw, 3.25rem);
    }

    .service-card {
        position: relative;
        overflow: hidden;
        transform-style: preserve-3d;
        border: 1px solid rgba(53, 100, 255, 0.92);
        border-radius: 0.92rem;
        background: linear-gradient(118deg, #1c1f29 0%, #12141b 48%, #0b0c12 100%);
        min-height: 182px;
        display: grid;
        align-items: start;
        gap: 0.52rem;
        padding: 1.2rem 1.15rem 1.1rem;
        box-shadow: 0 0 0 1px rgba(56, 95, 255, 0.2) inset, 0 0 24px rgba(35, 78, 255, 0.26), 0 10px 24px rgba(6, 12, 31, 0.55);
        transform-style: preserve-3d;
        transition: transform 220ms ease;
        will-change: transform;
    }

    .tilt-layer {
        transform-style: preserve-3d;
        transition: transform 220ms ease;
        will-change: transform;
    }

    .service-card::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: inherit;
        pointer-events: none;
        box-shadow: 0 0 0 1px rgba(70, 112, 255, 0.52) inset;
    }

    .service-card::after {
        content: "";
        position: absolute;
        left: 0;
        right: 0;
        bottom: 0;
        height: 22px;
        border-radius: 0 0 0.92rem 0.92rem;
        background:
            linear-gradient(180deg, rgba(34, 76, 224, 0) 0%, rgba(32, 74, 220, 0.42) 100%),
            radial-gradient(circle at 1px 1px, rgba(58, 116, 255, 0.65) 1px, transparent 1px);
        background-size: 3px 3px;
        opacity: 0.6;
        pointer-events: none;
    }

    .service-icon {
        width: 28px;
        height: 28px;
        border-radius: 0.45rem;
        display: grid;
        place-items: center;
        background: linear-gradient(140deg, #1d49db, #3058f6);
        border: 1px solid rgba(123, 153, 255, 0.92);
        box-shadow: 0 0 18px rgba(116, 145, 255, 0.46);
        position: relative;
    }

    .service-icon::after {
        content: "";
        position: absolute;
        inset: -10px;
        border-radius: 0.7rem;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.48), rgba(255, 255, 255, 0));
        filter: blur(2px);
        z-index: -1;
    }

    .service-icon::before {
        content: "";
        width: 10px;
        height: 10px;
        border: 1.5px solid rgba(246, 249, 255, 0.95);
        border-radius: 999px;
    }

    .service-card h3 {
        margin: 0.62rem 0 0;
        font-size: 1.95rem;
        line-height: 1.12;
        font-weight: 600;
        color: rgba(246, 248, 255, 0.96);
    }

    .service-card p {
        margin: 0;
        font-size: 0.82rem;
        line-height: 1.44;
        color: rgba(233, 239, 255, 0.9);
        max-width: 31ch;
    }

    .process-grid {
        margin-top: 1.6rem;
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: 0.8rem;
    }

    .process > .eyebrow,
    .process > h2 {
        text-align: center;
        margin-inline: auto;
    }

    .step-card {
        border-top: 1px solid rgba(55, 66, 99, 0.28);
        padding-top: 0.9rem;
        position: relative;
    }

    .step-dot {
        width: 24px;
        height: 24px;
        border-radius: 999px;
        border: 2px solid #3151ff;
        box-shadow: 0 0 0 3px rgba(74, 103, 255, 0.15);
        background: #f4f7ff;
    }

    .step-number {
        margin: 0.75rem 0 0.5rem;
        font-size: 1.3rem;
        font-weight: 700;
        letter-spacing: 0.02em;
    }

    .step-card h3 {
        margin: 0;
        font-size: 0.96rem;
    }

    .step-card p {
        margin: 0.45rem 0 0;
        font-size: 0.82rem;
        line-height: 1.45;
        color: rgba(23, 30, 48, 0.78);
    }

    .portfolio-row {
        margin-top: 1.7rem;
        display: grid;
        grid-auto-flow: column;
        grid-auto-columns: minmax(170px, 190px);
        gap: 0.8rem;
        overflow-x: auto;
        padding-bottom: 0.7rem;
    }

    .portfolio > .eyebrow,
    .portfolio > h2 {
        text-align: center;
        margin-inline: auto;
    }

    .project-card {
        border-radius: 0.9rem;
        border: 1px solid rgba(67, 77, 104, 0.22);
        background: linear-gradient(170deg, rgba(253, 254, 255, 0.98), rgba(243, 246, 255, 0.95));
        padding: 0.55rem;
        min-height: 280px;
    }

    .project-card img {
        width: 100%;
        height: 150px;
        object-fit: cover;
    }

    .project-card h3 {
        margin: 0.75rem 0 0;
        font-size: 0.9rem;
    }

    .project-card p {
        margin: 0.35rem 0 0;
        font-size: 0.75rem;
        color: rgba(23, 30, 48, 0.78);
        line-height: 1.4;
    }

    .team {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
        align-items: center;
    }

    .team-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.5rem;
        max-width: 320px;
    }

    .block {
        aspect-ratio: 1;
        border-radius: 0.7rem;
        background: linear-gradient(180deg, rgba(21, 26, 38, 0.18), rgba(11, 15, 25, 0.34));
    }

    .three,
    .four {
        border-radius: 999px;
    }

    .floating-cta {
        border-radius: 0.9rem;
        padding: 2rem 1.6rem;
        background: linear-gradient(130deg, rgba(255, 255, 255, 0.92), rgba(240, 245, 255, 0.9));
        border: 1px solid rgba(82, 103, 161, 0.25);
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1rem;
    }

    .floating-cta p {
        margin: 0;
        max-width: 32ch;
        font-size: 1.2rem;
        line-height: 1.15;
    }

    .end-cta {
        text-align: center;
        padding: 0.7rem 0 2.2rem;
    }

    .end-cta h2 {
        margin-inline: auto;
    }

    .end-cta .actions {
        justify-content: center;
    }

    :global(html.dark) .eyebrow {
        color: rgba(229, 236, 255, 0.7);
    }

    :global(html.dark) .subtitle,
    :global(html.dark) .hero-meta,
    :global(html.dark) .step-card p,
    :global(html.dark) .project-card p,
    :global(html.dark) .service-card p {
        color: rgba(229, 236, 255, 0.76);
    }

    :global(html.dark) .btn-ghost {
        border-color: rgba(116, 136, 255, 0.45);
        color: #dce4ff;
        background: rgba(30, 36, 55, 0.65);
    }

    :global(html.dark) .service-card,
    :global(html.dark) .project-card,
    :global(html.dark) .floating-cta {
        border-color: rgba(94, 120, 214, 0.32);
        background: linear-gradient(180deg, rgba(15, 20, 35, 0.92), rgba(14, 18, 31, 0.76));
        box-shadow: 0 0 0 1px rgba(47, 77, 180, 0.24) inset;
    }

    :global(html.dark) .services {
        background: linear-gradient(180deg, #04070f 0%, #060915 100%);
    }

    :global(html.dark) .scroll-frames-sticky {
        background: transparent;
    }

    :global(html.dark) .frames-loader {
        border-color: rgba(112, 138, 209, 0.32);
        color: rgba(230, 238, 255, 0.9);
        background: rgba(12, 20, 39, 0.72);
    }

    :global(html.dark) .step-card {
        border-top-color: rgba(173, 188, 255, 0.2);
    }

    :global(html.dark) .step-dot {
        background: #080f20;
    }

    :global(html.dark) .block {
        background: linear-gradient(170deg, rgba(198, 209, 251, 0.2), rgba(40, 51, 82, 0.35));
    }

    :global(html.dark) .team {
        background: linear-gradient(180deg, rgba(6, 10, 19, 0.75), rgba(6, 10, 19, 0.26));
        border-radius: 1rem;
        padding: 1.4rem;
    }

    :global(html.dark) .home {
        gap: 5.9rem;
    }

    @media (max-width: 960px) {
        .hero,
        .team {
            grid-template-columns: 1fr;
        }

        .hero {
            padding-top: 1rem;
        }

        .hero-phone {
            width: clamp(220px, 58vw, 300px);
            height: clamp(360px, 72vw, 470px);
        }

        .hero-visual {
            min-height: 460px;
            margin-top: 0.8rem;
        }


        .process-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .scroll-frames {
            height: 520vh;
        }
    }

    @media (max-width: 760px) {
        .home {
            padding-top: 1.2rem;
            gap: 3.4rem;
        }

        .services-grid {
            grid-template-columns: 1fr;
            gap: 1rem;
        }

        .process-grid {
            grid-template-columns: 1fr;
        }

        .scroll-frames {
            height: 460vh;
        }

        .floating-cta {
            flex-direction: column;
            align-items: flex-start;
        }
    }
</style>
